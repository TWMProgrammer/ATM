/**
 * Supported languages for custom Auto-Tag engines.
 * HTML, XML, and React (TSX/JSX) are already handled natively.
 * We only add languages not supported by default (e.g., Markdown).
 */
export const SUPPORTED_LANGUAGES = ['markdown'];

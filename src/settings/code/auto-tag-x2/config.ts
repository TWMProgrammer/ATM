/**
 * Lenguajes soportados para los motores personalizados de Auto-Tag.
 * HTML, XML, y React (TSX/JSX) ya son manejados nativamente por VS Code con excelente rendimiento.
 * Solo agregamos aquí los lenguajes donde VS Code no lo soporta por defecto (ej. Markdown).
 */
export const SUPPORTED_LANGUAGES = ['markdown'];
